name: CI

on:
  push:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      DATABASE_URL: ${{ secrets.NEON_DATABASE_URL }}
      PGSSLMODE: require
      CI: true
      JWT_SECRET: ${{ secrets.JWT_SECRET }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Enable Corepack
        run: corepack enable

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: false

      - name: Get pnpm store directory
        shell: bash
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Cache Turbo
        uses: actions/cache@v4
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-turbo-

      - name: Run migrations
        run: pnpm --filter @workspace/infrastructure run db:migrate

      - name: Run tests
        run: pnpm turbo run test --filter=!@workspace/e2e-tests --cache-dir=.turbo

  # uncomment the following lines if you want to deploy to vercel and only if the test job is successful
  # deploy:
  #   needs: test
  #   runs-on: ubuntu-latest
  #   if: success()
  #   steps:
  #     - name: Trigger Vercel deploy
  #       run: |
  #         if [ "${{ github.ref }}" == "refs/heads/staging" ]; then
  #           curl -X POST "${{ secrets.VERCEL_DEPLOY_STAGING_HOOK }}"
  #         elif [ "${{ github.ref }}" == "refs/heads/main" ]; then
  #           curl -X POST "${{ secrets.VERCEL_DEPLOY_MAIN_HOOK }}"
  #         fi
