PGHOST='localhost'
PGPORT=5433
PGDATABASE='collaborateam_test'
PGUSER='postgres'
PGPASSWORD='postgres_password'
PGSSLMODE='disable'

# In Production : We will use Neon branch for integration tests (we'll provide these infos in github secrets)
# Uncomment and configure these to use Neon instead of local PostgreSQL
# PGHOST='ep-xxx.us-east-2.aws.neon.tech'
# PGDATABASE='avionics'
# PGUSER='your-user'
# PGPASSWORD='your-password'
# PGSSLMODE='require'